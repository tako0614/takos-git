{
  "name": "takos-git",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Standalone Git Smart HTTP service (installable Capsule).",
  "scripts": {
    "build:worker": "bun run scripts/build-worker.ts",
    "smoke:postdeploy": "bun run scripts/post-deploy-smoke.ts",
    "test": "bun test",
    "check": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
